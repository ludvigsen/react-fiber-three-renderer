import ReactThree from './src/ReactThree';
export default ReactThree;
/*const render = require('./src/renderer/render');

const WordDocument = require('./src/components/WordDocument');
const Document = require('./src/components/Document');
const Text = require('./src/components/Text');

const root = new WordDocument();

const document = new Document(root);
const text = new Text(root);
text.appendChild('test123');
document.appendChild(text);

render(document, 'test123.docx');*/

/*function render(element, target, callback) {
  CustomRenderer.injectIntoDevTools({
    bundleType: 1,
    version: '0.1.0',
    rendererPackageName: 'webgl-renderer',
    findHostInstanceByFiber: CustomRenderer.findHostInstance,
  });
}
*/
